import java.util.Scanner;

public class Polygon extends Point {
    Scanner sc = new Scanner(System.in);
    int numVertices = sc.nextInt();
    int []xPos = new int [numVertices];
    int []yPos = new int[numVertices];

    for(int i = 0; i < numVertices; i++){

    }

}
